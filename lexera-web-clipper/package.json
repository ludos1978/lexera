{
  "name": "lexera-web-clipper",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-browser web clipper for Lexera kanban boards",
  "dependencies": {
    "@lexera/shared": "file:../packages/shared"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "node scripts/build.mjs --clean",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "jsdom": "^29.0.2"
  }
}
